# 📅 Отчет за [2026-04-18]

**⏱ Время в фокусе:** ~5.5 часов


<!-- def friend(x):
    #Code
    friends_list = []

    for name in x:
        if len(name) == 4:
            friends_list.append(name)
    return friends_list


input = ["Ryan", "Kieran", "Jason", "Yous"]

print(friend(input)) -->

Задача довольно простая, поначалу решил использовать enumerate, но быстро понял,что только что прочитанное, не всегда нужно применять, добавить нечего, некст.


<!-- def solution(text, ending):
    return text[-len(ending):] == ending -->


Попробовал решить задачу через while  text[-2:] == ending[-2:] , вышла глупость, потому как  символово для проверки в конце может быть 1,2,3 и т.д . Оказывается в срезах можно использовать не только цифры, но и заданные значения, как в примере , то есть сравнение длины конца с концом. Ещё думал len() выдаёт цифры,да ещё и - ,но на самом деле это лишь сравнивает кол-во элементов равное переменной справа.
Ещё вариант endswith () , сравнивающий  заканчивается листрока на указанную подстроку . то есть     файл = "отчет.pdf"

# Проверка, что файл — PDF
если файл.endswith(".pdf"):
    print("Это PDF файл.")
иначе:
    print("Это не PDF файл.")



<!-- <!-- def longest(a1, a2):
<!--     
    set1 = set(a1)
    set2 = set(a2)
   

    s3 = set1.union(set2)
    sorted_set = "".join(sorted(s3))
    return sorted_set
a = "xyaabbbccccdefww"
b = "xxxxyyyyabklmopq"     --> 

<!-- print(longest(a,b)) --> 

"""Помогло знание, что для объединения уникальных элементов в один список, можно использовать set(),union or | .
  Ещё раз, более осмысленно, sorted() работает со всеми типами контейнеров(списки, кортежи,множества), типо внутри без разницы , дальше уже по знакому примеру .join"""




  <!-- def add_binary(a,b):
    с = a + b
    result = bin(с)

    return result[2:]

a = 11
b = 1 
print(add_binary(a,b)) -->


"""Первый затуп при сложении, сналача перевёл в двоичную систему, а потом сложил, получается сложение текста, нет.
 При переводе в разные системы исчисления добавляется префикс, в данном случае 0b ,поэтому используется срез для удаления и более
 Функция format(value, format_spec) берет любое значение и превращает его в строку, оформленную по твоим правилам.
 То есть вот такая штука  берет 1 аргументов необходимое действие,а в 2 нужный формат, в данном случае 'b' - что означает binary"""


<!-- def find_next_square(sq):
    # Return the next square if sq is a square, -1 otherwise
    
    root = sq ** 0.5

    if root.is_integer():
        next_root = root + 1
        return int(next_root ** 2)
    return -1


example = 24
print(find_next_square(example)) -->

"""Задача с квадратами не далась. 
ДЛя вычисления корня необходимо число ** 0.5 , дальше идёт проверка, если число целое, то прибавляется 1 и возводится  в степерь,если нет, то возвращается -1 """



<!-- def is_triangle(a, b, c):

    if a + b > c and a + c > b and b + c > a:
        return True 
    else:
        return False  -->
    

""" В треугольнике любая сторона должны быть короче сумм двух других"""

