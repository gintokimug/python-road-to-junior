# 📅 Отчет за 25.02.2026


1. **Python**
            `ef factorial(x):' `
    `"""This is a recursive function`
    $`to find the factorial of an integer"""`

    `if x == 1:`
        `return 1`
    `else:`
        `return (x * factorial(x-1))`


`num = 3`
`print("The factorial of", num, "is", factorial(num))`

   Спуск (Заморозка)Компьютер «замораживает» вычисления и открывает новые уровни:Вызов factorial(3): «Так, мне нужно $3 \times \text{factorial}(2)$. Замораживаю тройку, иду считать factorial(2)».Вызов factorial(2): «Так, мне нужно $2 \times \text{factorial}(1)$. Замораживаю двойку, иду считать factorial(1)».Вызов factorial(1): «О, тут if x == 1. Возвращаю 1».   

   Как понял рекурсия опускается до низшего значения, не выполняя  действие в скобках, пример выше, и только после выполнения условия if равно true, поднимается наверх, производя необходимое действие. `НУЖНО ПОСМОТРЕТЬ ПРИМЕРЫ ЗАДАЧ НА РЕКУРСИЮ`.    


2. **Python**

        Символ *, при использовании данного символа в аргументах функции, позоляет использовать произвольное количество символов в функции. Произвольное количество аргументов позволяет передавать разное количество значений при вызове функции.
НО, так же использование этого символа при использовании import позволяет импортировать ВСЁ, так понял в некоторых случаях это необходимо,а  в некоторых лишнее, потому что опять же нагружено всё, а в этом нет необходимости.

3. **Python**
        Ознакомился с понятием модули и пакеты. Важная тема для базы т.к. придётся в дальнейшем в больших проектах, large code , использовать большое количество функций, для распределения сделаны модули и пакеты.
        Пример: в модуле ты проводишь определённое действие, функцию,  которая может потребоваться в дальнейшем.ЧТобы  не переписывать всю логику буду использовать import , пример:
        
`from math import pi`

`print(pi)`

 `Output: 3.141592653589793`

        Ещё здесь же есть функиця dir() коротрая может показать всё, что находится в папке,тут важное разделение есть файлы с припиской __Умпа-Лумпа__ и файлы с обычными 'абв' (это файлы используемые/добавленные тобой) , и вот это аттрибуты Python по умолчанию(до этого думаю дойду)
        Ещё здесь важный момент ,как объяснило 2 видео(индус и ру парень) файл __init__. py , это необходимо, чтобы директория(она же dir, directoty, каталог) считалась пакетом, то есть внутри каждого  пакета должен быть такой файл, он может быть пустым, но как правило, в него что то засовывают, тут посмотрел видео с большой(Хер его знает насколько). Имея корневую папку Main, ты не будешь хранить в ней всё,поэтому используешь модули и пакет. Вот ещё момент * = _all_ , то есть при импорте ты добавляешь всё, НО, так же ты можешь задать критерий _all_ = a, b, d  etc.  Типо  ты импортируешь всё, что засунешь во всё.... как бы это не звучало 

`Модуль`        Модуль это файлы с припиской .py , которые , как правило, содержатся в main, в них описываются функции


`Пакет`         (хотя в VS Code при создании пишется folder). Это директория, содержащяя в себе файлы с припиской .py, 
        Имея корневую папку Main, ты не будешь хранить в ней всё,поэтому используешь модули и пакет.
