# 📅 Отчет за [2026-04-24]

**⏱ Время в фокусе:** 6.5 часов

def sort_by_length(arr):
# sort(метод списка),  sorted(встроеннная функция)
# передавать аргументом key = ...в зависимости от запроса: len(), abs(), .lower,lambda.
    arr.sort(key = len)
    return arr
    
# Оператор == ищет полное совпадение, а in проверяет наличие символа в любой позиции (начало, середина, конец).




def arithmetic(a, b, operator):
# В данном случае использование словарей куда эффективнее, нежели  кучка  if, работе со словарями ужно уделить больше внимания
    dictionaty = { 
                'add': a + b,
                'subtract': a - b,
                'multiply': a * b,
                'divide': a /b
                     }
    return dictionaty[operator]


    
def dont_give_me_five(start,end):
# Оператор == ищет полное совпадение, а in проверяет наличие символа в любой позиции (начало, середина, конец).
    result = 0
    for number in range(start,end + 1):
        if '5' not in str(number):

            result += 1
    return result



    def break_chocolate(n, m):
# Важный момент, ответ в нахождении закономерности, просто увидел,всё.
    if n <= 0 and m <= 0:
        return 0
    else:
        return (n * m) - 1
    

n = 7
m = 4
print(break_chocolate(n,m))