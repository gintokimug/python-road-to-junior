# 📅 Отчет за 24.02.2024


##  Что изучил
1. **Python**
    Повторил множества, он же set(), он же {}. Они изменчивы, но неупорядочены, поэтому нельзя получить доступ по индексу. В них можно добавить элемент, список можно засунуть в множество. Множества можно складывать, выделять общие/разные элементы, что интересно, те же операции можно проводить с библиотеками.

2. **Python**
    Метод enumerate() . Так понял , он может список, кортеж превратить в dict(), где по умолчанию  все становится по типу ключ=значение (причём даётся порядковый номер от 0 до ...)

3. **Python**
    Ещё разобрался в разнице методово sort() и sorted 
    1-ый работает только со списками и после применения метода навсегда изменяет его.
    2-ой работает с разными типами данных(списки,кортежи,словари, множества). Важно, создаётся отсортированный объект,НО, старый остаётся неизменным, что важно так понимаю и на выходе всегда даёт список. 

4. **Python**
    Работа с словарями. Они могут складываться путём update() или | ,ещё момент, если у тебя есть одинаковые данные в разных словарях, браться будет последнее. КЛЮЧИ должны быть неизменяемыми , то есть мы можем их только создать, это int, tuple, str. Значениями может быть любой тип данных(пока это так запомнил). Тоже кол-во методово работы достаточно, можно брать все value, key. Можно добавлять элемент через :
    **country_capitals["Italy"] = "Rome"**
    По такому же принципу можно заменить значение в словаре

5. **Python**
    Ещё встретил del 
    С помощью неё можно удалять что угодно, НО, понял, что проще пользоваться всякими  remove() , pop(). 

6. **Python**
    Функции. Ещё раз,  проверки и цикл, для работы проще создавать маленькие функции, с ними легче работать и их легче корректировать. Узнал, что можно сделать пустой принт, но если заранее выдал в аргументах значение, он их и отдаст, а если добавишь своё, то поменяет, для меня это новость. Ещё момент, условно **def example(N) а потом пишешь example(i)** , это будет одно и тоже, типо N это просто маркер, что хочешь, то и делай. 
    *""Функции всё равно, как называлась переменная «снаружи» (i, numb или val). Она просто берет значение и присваивает его своему внутреннему имени N.""* Gemini
    Ну и прикольная фишка, ты можешь прописать всю задачу внутри функции, весь процесс, при вызове просто указываешь переменную,БАМ, всё работает.
    Принт писать не надо(кринж типо) надо return, привыкну.

7. **Python**
    Про импорт ещё поболе узнал. По учебнику работая, ипользовал random, теперь увидел math. Ты можешь найти корень числа, возвести в степерь . То есть функции пользователя( что пишу я), а есть библиотечные( что  делает за тебя).
    Ещё появилась *args (arguments) и **kwargs. Непонятно, берут числа, слова, про экономию памяти, боле пока не понял.Вернусь.

8. **Python**
    Ну и последнее 3 вида области действия переменных. Локальные, глобальные и nonlocal (прям там писать надо). ТЫ не можешь изменить глобальную внутри функции, только если пометишь её как nonlocal.


9. **Git**
    Создал свой 1 репозиторий:D. Начал вести журнал, сделал поток между гитом и папкой своей, для отчётности по результатам. Понял, что можно делать это из VScode, можно из командной строки, или даже PowerShell, **завтра спрошу** . Сделал свой первый commit, понял слово пушить, то бишь закинуть,чтобы делиться и работать совместно.

### Планы на 25.02

- finished Python Functions
- finished Python Files
finished Python Exception Handing

По времени не уверен, но если что пробегусь галопом, потом вернусть

- Установить Cloude , много рилсов , `надо прочекировать` 
