# 📅 Отчет за [2026-04-30]

**⏱ Время в фокусе:** ~2  часов


def century(year):
# Использование математических формул порой эффективнее, чем циклов
    if year % 100 == 0:
        return year // 100
    else:
        return (year // 100) + 1
example = 88
print(century(example))
