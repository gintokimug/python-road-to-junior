<!-- def find_short(s):
    # your code here
    words = s.split()
    min_len = len(words[0])
    for word in words:
        if len(word) < min_len:
            min_len = len(word)
    return min_len -->


      """для сравнения слов на их длину нужно использовать .split(), так он разбивает целую строку на отдельные слова по которой можно пройтись циклом с помощью len()
      Так же необходимо всегда заводить переменную для сравнения,куда  ты будешь класть в последствии что либо , это  МАСТ ХЕВ, разумеется есть решение однострочное 
      return min(len(word) for word in s.split())"""
      

<!-- 
# def divisors(n):
#     count = 0
#     for numbers in range(1,n + 1):
#         if n % numbers == 0:
#             count += 1

#     return count
        
# example = 5
# print(divisors(example)) -->


"""Решение задачи не давалось, потому что не поставил range (1 , n + 1) а я ставил ( n + 1), он вендь стартует с 0, ну и я пробовал просто делить, а оказалось вот так, ещё
Ещё 2 решение, при поиске делителей мы брали попарные числа, т.к. оба дают нужный результат из чего следовало,что стоит дойти до корня и это всё, дальше будут зеркальные отражения. """    




<!-- def maskify(cc):
    if len(cc) <= 4:
        return cc
    else:

        count = '#' * (len(cc) - 4)
        tail = cc[-4:]
        result = count + tail

    return result


def maskify(cc):
    return ('#' * (len(cc)- 4) + cc[-4:])


example = "aaaabbbbcccc"
print(maskify(example)) -->

""" 1 ошибка была в использовании среза, использовал [:-4] ,а надо было [-4:], потому что мой вариант выдал бы 4 цифры, которые нужно спрятать, а не наоборот
2 ошибка была использование цикла, здесь  бьстрее было использовать атомарные(неделимые) операции, проще говоря одним разом, нежели по кусочку через циклы """