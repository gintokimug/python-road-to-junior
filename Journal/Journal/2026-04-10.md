📅Отчет за 10.04.2026

«Синтаксис in универсален, но его производительность зависит от структуры данных. Поиск x in list — это $O(n)$, поиск x in set — это $O(1)$. Для маленьких наборов (как 5 гласных) разница незаметна, но на больших словарях это критически важно».

«Обработка сырых данных: Данные часто приходят в виде строк (из API, файлов или баз). Первый шаг — это всегда нормализация: split() для разделения и map(int) для приведения типов. Только после этого можно применять агрегатные функции вроде max() и min(), которые работают со сложностью $O(n)$».

# def double_check(strng):
#    s = strng.lower()
#   for x in range(len(strng)- 1):
#        if s[x] == s[x + 1]:
#            return True 
#    else:
#        return False**
        
Почти сам решил задачу, возникала проблема выхода за рамки, долго не мог сообразить, оказалось всё проще, ты идешь до предпоследнего элемента, а уже внутри цикла можешь проверить и последний.


# def check_coupon(entered_code, correct_code, current_date: str, expiration_date: str) -> bool:
#    if entered_code is not correct_code and entered_code != correct_code:
#        if type(entered_code) != type(correct_code):
#            return False
#
#   fmt = "%B %d, %Y"
#    cur = datetime.strptime(current_date, fmt)
#    exp = datetime.strptime(expiration_date,fmt)
#
#    return (entered_code == correct_code) and (type(entered_code) == type(correct_code)) and (cur <= exp)

Узнал про импортирование datetime и метод strptime,нужно передавать 2 аргумента(  первый: сама строка, второй:  шаблон для понимания где месяц,день и т.д.) ПРи написании используется % , это как маркер, обозначений куча, заучивать пока не хочу. Применение метода = ты пишешь импортируемый модуль.импортируемый метод (2 аргумента). 

# def descending_order(num):
#    s = str(num)
#    sorted_list = sorted(s, reverse=True)
#    str_lst = ''.join(sorted_list)
#   return int(str_lst)

Долго возился с задачей так как не  мог понять, почему <list_reverseiterator object at 0x000002B0B96ABD30> возвращает такое, в задачах на сортировку по убыванию или в целом  с числами, необходимо провести несколько трансформаций,а именно: int - str - list() - str - int. Не мог сообразить,а по факту необходимо просто преобразовывать.