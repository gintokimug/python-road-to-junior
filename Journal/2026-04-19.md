# 📅 Отчет за [2026-04-19]

**⏱ Время в фокусе:** ~5.5 часов

<!-- def row_sum_odd_numbers(row):
    return row ** 3

    # number =2
    # result = 1
    # row = row // 2
    # for number in range(row[1::2]): -->


    """Обращай внимание на построение задач, особенно полезно найти какую либо закономерность, в таком случае всегда может найтись формула для ее решения
    В данной задаче это возведение в  ** 3"""

<!-- def binary_array_to_number(arr):

    result = ''.join(map(str, arr))
    return int(result, 2)   

Testing = [1, 1, 1, 1] 
print(binary_array_to_number(Testing)) -->

"""Перевести число из двоичной в десятичную систему. Дан список, его нужно конвертировать в строку, тоесть делаем ''.join
затем используем map (1 аргументом хотим увидеть формат , то  есть стр, 2 аргументом объект конвертируемый в строку)
Ещё  момент ,что у int() может быть 2 аргумента, вторым  можно добавлять систему исчисления, то есть от 2 до 36
ТО есть, сначала мы список превращаем в строку, а уже потом в число
Ещё комментарий по map(), 1 аргументом нужно действие делаемое с каждым элементом, это может быть abs(пофиг на -), это может быть len(длина интересующего объекта,
А может быть и сама функция ( то есть действие ты прописал заранее, а теперь применяешь его к итерируему объекту)
НУ и последнее, ты обязательно должен оборачивать map во что нибудь, это может быть [], ''.join(), думаю это ещё не всё """



<!-- def number(bus_stops):
    people = 0
    for pas in bus_stops:
        people += pas[0]
        people -= pas[1]
    return people -->

""" Включил неожиданно сам для себя ИИ агента, теперь нужно выключить,потому что решение задачи становится неактуальным
Решение задачи  в обращении по индексу,  а не элементу, т.к. неизвестно сколько будет остановок."""



<!-- def divisors(integer):
    result = []
    for x in range(2, integer):
        if integer % x == 0:
            result.append(x)
    if not result:
        return f'{integer} is prime'

    return result
    
example = 12
print(divisors(example)) -->


"""1 ошибка была в выставлении границ range, надо было брать от 2 до переменной, потому что по условиям задачи. Так же ошибка в выборе делителя/делимого, казуально, не забывай
Напомню себе ещё раз про .append(), метод работает с списками."""

    