📅Отчет за 11.04.2026


# def filter_list(l):
#     flist = list(l)
#     new_list = []
#     for element in l:
#         if element == str:
#             flist.pop(element)
#         else:
#            new_list += element 


<!-- def filter_list(l):
    new_list = []
    for element in l:
        if type(element) is int:
            new_list.append(element)
    return new_list -->



    В Python += для списков ожидает другой список (итерируемый объект). Запомнить при последующих операциях, += ожидает тип объекта такой же ,как и влевой части . Затуп возник с  обращением к разным типам данных в однос списке и последующем добавлении, запомнил += отстой, если есть .append , ну и проверка типа 2 способами, серез type(element) или isinstance( объект, тип)  и в итоге задача может решаться через генератор списка . 


#    def is_square(n):
#    if n < 0:
#        return False
#    elif n == 0:
#        return True 
#    
#    for num in range(n+1):
#       if n == num * num:
#            return True
#    return False 

<!-- def is_square(n):
    if n < 0: return False
    if n == 0: return True 

    root = n ** 0.5
    return root % 1 == 0 -->


   ПРОСТО 2 МОМЕНТА RETURN В ЦИКЛЕ FOR  ВСЕГДА НУЖНО СТАВИТЬ  НАРАВНЕ , НЕЛЬЗЯ ВНУТРИ, ОН НЕ  БУДЕТ ВЫХОДИТЬ, ЗАПОМНИ УЖЕ!!!!
 ВТОРОЙ МОМЕНТ В  RANGE ВСЕГДА,ВСЕГДА,ВСГДА СТАВЬ + 1, ЧТОБЫ ВЗЯТЬ САМО ЧИСО ИНАЧЕ НЕ ВЫЙДЕТ, В ЗАДАЧЕ ЦИФРА 4 ШЛА ДО 3, Т.К ИНДЕКСАЦИЯ С 0 МАРТЫШка

 И при моем решении ответ был верен, но расчёт упал т.к. много операция, это в тему О (n) O(1) , что о1 всегда лучше, потому что не тратит мног ресурсов для решения .
 оптимальное решение было с  корнем, тут надо понять  алгорим, допусти 2 ** 0.5 = 1, 1 % 1 == 0, допусти 4 ** 0.5 = 2, 2 % 1 == 0, подходит,а вот поставь я 3, 3 ** 0.5 = 1.5, 1.5 % 1 != 0, вот, ТАК ЖЕ ВАЖНО ПРОВЕРЯТЬ ЗНАКИ ПРЕПИНАНИЯ, поставил запятую и всё полетело , только ИИ помогла .



# def odd_or_even(arr):
#
#    sumarr = sum(arr)
#    if sumarr % 2 == 0:
#        return "even"
#    else:
#        return "odd"

Максимально глупый затуп, вместо % ставил // , стыдно, но напоминаю, % остаток от деления ,  для задач это 0 или 1 , а // это сколько целых частей ты можешь получить от делимого числа И это разумеется можно было решить через тернарный оператор  Check:

return "even" if sum(arr) % 2 == 0 else "odd"
