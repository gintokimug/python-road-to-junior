# 📅 Отчет за 23.02.2026


##  Что изучил
1. **Python** 
    Вновь сделал задачу на фибоначи. Смог разобраться в …копируемости переменных?... типо  а, б = б, а+ б, вообще понял, что это такое, это сумма двух предыдущих чисел,думаю с переменными пригодится.

2. **Python**
    Снова сделал задачу на факториал числа, тоже узнал,что это такое. Был затык на проход цикла, не понимал как происходит  перезапись ,вообще довольно просто, но понимание далось с трудом.

3. **Python**
    Вновь прошёлся по самой самой базе, это виды переменных,  что некоторые из них могут стакаться (инт + флоат) .Повторил  списки, кортежы и ещё пройдусь по множествам и словарям.
    - Списки (`list` / `[]`) Изменяемые, мощный функционал, медленнее.
        Изменяем,  огромный функционал для действий , добавить элемент, убрать, прочитать наоборот, добавить счётчик, внутри могут быть разные переменные 
    - Кортежи (`tuple` / `()`) Неизменяемые, легкие, быстрые. Можно конвертировать в список и обратно.
        Неизменяем, проделать такое же кол-во действий как и со списком нельзя, НО, можно преобразовать в список.

4. **Python**
    Кстати, понял, поверхностно пока, как работает функция (это отдельный код для работы) который работает как бы отдельно от кода, аля инструкция для решения бизнес логики. 

5. **Python**
    Поверхностно коснулся генераций , точнее yield, так понял он полезнее для скорости, т.к. объемы памяти и что то подобное, в рамках   1 000 000  итерация это может помочь. 

6. **Python**
    Ещё увидел новые методы id который показывает место в памяти каждого объекта, ещё забыл название который показывает скорость решения задачи ( на примере () и tuple())    


7. **Git:** Разобрался с `init`, `add`, `commit`. Понял разницу между локальным и удаленным репозиторием.

## 🧠 Инсайт дня (AHA!)
Понял механику цикла в факториале: переменная не просто хранит число, она "наматывает" на себя результат с каждым шагом. А `yield` — это способ не создавать огромный список в памяти, а выдавать значения по одному "по требованию".

## 🛠 Практика
- Написал генератор чисел Фибоначчи.
- Реализовал расчет факториала через цикл.
- Экспериментировал с мутабельностью списков и иммутабельностью кортежей.

## ⛔️ Затык
Сложно далось понимание перезаписи переменных внутри цикла (факториал). Пришлось проследить выполнение кода пошагово, чтобы понять, как меняется значение на каждой итерации.

## 🎯 План на завтра
1. Разобраться с Множествами (`set`) и Словарями (`dict`).
2. Настроить систему отчетов (GitHub + Markdown).



