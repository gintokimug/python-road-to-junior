📅Отчет за 12.04.2026


<!-- def accum(st):
    result = []

    for index, letter in enumerate(st, start= 0):
        point = (letter *(index +1)).capitalize()
        result.append(point)
    return "-".join(result)

example = ("abcd")
print(accum(example)) -->


"""Возился с задачей чуть больше 2 часов, не мог сообразить как вклинить логику  цикла на написании нескольких букв одновременно.
   Важный косяк, снова пробую делать += ,когда нужно делать .append(), ЗАПОМНИ ЗАПОМНИ ЗАПОМНИ
   Встречал ранее, теперь осознанно , enumerate() ,где в цикле ты можешь  указываать элемент итерируемого объекта и  его индекс, в качестве аругементов 1= сам объект, 2 = откуда стартуешь)
   
"""


<!-- def is_isogram(string):
    string = string.lower()
    if len(string) > len(set(string)):
        return False
    else:
        return True -->
    
<!-- def is_isogram(string):
    for char in string:
        if string.count(char) > 1:
            return False
        else:
            return True  -->
        
<!-- def is_isogram(string):
    seen_letters = []
    for char in string:
        if char in seen_letters:
            return False
        else:
            seen_letters.append(char)
            return True -->

"""Снова нерешшная задача, пытался додуматься как сравнить букву с буквой в строке, точнее её количество и если больше 1, False. 
    В случае сравнения повторений можно использовать множество - ведь это неупорядоченные, но единичные элементы
    Ещё узнал/вспонил про метод .count() применяется к строкам и выводит как раз таки счётчик , что и было нужно 
    Ну и 3 способ это создание пустого списка, и если буква там есть выход,если нет , добавляем и идём дальше, что и выдаёт правильный ответ """


<!-- 
    <!-- def xo(s):
    s.lower()
    return (s.count('x') == s.count('o')) --> 

    Задача решилась благодаря знанию метода .count для строк, приятно. Попробовал функцию debug , работает с циклами где можно пошагово проерять что происходит и смотреть какие измененния данных  или значений происходит. 
    

<!-- def to_jaden_case(string):

    words= string.split()
    result = []

    for word in words:
        result.append(word.capitalize())
    return "".join(result) -->


    Ошибки в задаче были с append, нельзя добавлять внутри цикла может привести  ктому, что упадет весь цикл, делать это надо в return. Ещё делал capitalize просто к words, необходимо либо добавлять генератор , либо добавлять результат  в новый список.

